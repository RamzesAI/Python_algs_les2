'''
Задание 1. Написать программу, которая будет складывать, вычитать, умножать или делить два числа.
Числа и знак операции вводятся пользователем. После выполнения вычисления программа не должна завершаться,
а должна запрашивать новые данные для вычислений.
Завершение программы должно выполняться при вводе символа '0' в качестве знака операции.
Если пользователь вводит неверный знак (не '0', '+', '-', '*', '/'), то программа должна сообщать ему
об ошибке и снова запрашивать знак операции.
Также сообщать пользователю о невозможности деления на ноль, если он ввел 0 в качестве делителя.
'''
user_operation = None
while user_operation != '0':
    first_number = int(input('Введите первое число: '))
    second_number = int(input('Введите второе число: '))
    print('Для выполнения арифметической операции укажите действие:')
    print('Сложение:  +')
    print('Вычитание: -')
    print('Умножение: *')
    print('Деление:   /')
    print('\033[31mДля выхода введите "0"\033[0;0m')
    user_operation = input('Ваш выбор: ')
    while user_operation not in '+-*/0':
        print('Необходимо ввести указанные выше операции!!!')
        user_operation = input('Ваш выбор: ')
    if user_operation == '+':
        print(f'\033[33mСумма чисел = {first_number + second_number}\033[0;0m')
    if user_operation == '-':
        print(f'\033[33mРазность чисел = {first_number - second_number}\033[0;0m')
    if user_operation == '*':
        print(f'\033[33mПроизведение чисел = {first_number * second_number}\033[0;0m')
    if user_operation == '/':
        try:
            print(f'\033[33mЧастное чисел = {round(first_number / second_number, 5)}\033[0;0m')
        except ZeroDivisionError:
            print('На ноль делить нельзя!!!')